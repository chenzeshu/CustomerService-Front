const companies_status = ['签约', '未签约']
const service_status = ['待审核', '拒绝', '待派单', '已派单', '申请完成', '已完成', '申述中']
const channel_status = ['待审核', '运营调配', '已完成', '拒绝', '申述中']
const problem_status = ['未解决', '运维解决中', '技术或厂商解决中', '专家解决中', '已解决']

const company_param = ['type', 'count']
const other_param = ['status', 'count']
const finish_param = ['finish', 'count']
const problem_param = ['problem_step', 'count']


export default {
  company_param,
  other_param,
  finish_param,
  problem_param
}
